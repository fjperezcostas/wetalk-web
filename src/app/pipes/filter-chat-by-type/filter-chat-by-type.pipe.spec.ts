import { FilterChatByTypePipe } from './filter-chat-by-type.pipe';

describe('FilterChatByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChatByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
